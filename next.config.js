/** @type {{images: {remotePatterns: [{protocol: string, hostname: string}]}, experimental: {appDir: boolean}}} */
const nextConfig = {
  experimental: {
    appDir: true,
  },
  images: {

  }
}

module.exports = nextConfig
